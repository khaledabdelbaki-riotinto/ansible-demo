---
- name: Run inline Bash code in playbook
  hosts: all
  gather_facts: false
  tasks:
    - name: Execute Bash code directly
      ansible.builtin.shell: |
        echo "Hello from Bash embedded in playbook!"
        uname -a
